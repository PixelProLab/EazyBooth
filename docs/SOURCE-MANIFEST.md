# Source inclusion boundary

Reviewed publication roots: `src/`, `tests/`, `scripts/`, `docs/`, `installer/`, `templates/`, `assets/app.ico`, `.github/`, and root package/build/configuration/license/documentation files.

Excluded: `node_modules/`, build/installer outputs, `.npm-cache/`, `.evidence/`, profiles, captures, PIN/settings runtime data, guest photos, logs, private setup and vendor installer binaries. No Misk Git history or runtime data was copied into EazyBooth. The source retains its Pixel Pro Lab ISC notice and documents the reference commit.

The template contains only the three artwork files supplied for this task plus the editable event manifest. SHA-256:

| Asset | Hash |
| --- | --- |
| frame-1.png | `3D5D35DB4DCD1DAD835C5A0983B922022C69CBA5D71F2DD136593E2E0DAFEEE2` |
| frame-2.png | `A5ADD574AB94F76528F3FE9F0B5F63D0FA5FCD8630A3370B86AFAB98D0B7DCAB` |
| welcome.jpeg | `448B9CE94B03719ED6720F4037B8C6C44ECF77651FACB362B37B1607E9FB5891` |

The local source folder is named EasyBooth because that is the task's configured writable workspace. Product and GitHub repository names are EazyBooth. No application code depends on this workstation path.
